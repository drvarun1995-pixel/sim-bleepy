export const metadata = {
  title: 'Bleepy Weekly newsletter preview',
  robots: { index: false, follow: false },
}

export default function NewsletterPreviewLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
